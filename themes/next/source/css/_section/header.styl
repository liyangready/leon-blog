.headband {
  height: 3px;
  background: $headband-bg;
}

.header {
  background : url( /images/back.jpg) center #000;
  background-size : 100% ;
}

.header-inner { position: relative; }

.site-meta { text-align: $site-meta-text-align; }

.brand {
  position: relative;
  display: inline-block;
  padding: 0 25px;
  color: $brand-color;
  background: $brand-bg;
  border-bottom: none;
  font-family: $brand-font-family;
}

.logo {
  display: inline-block;
  margin-right: 5px;
  line-height: 36px;
  vertical-align: top;
}

.site-title {
  display: inline-block;
  vertical-align: top;
  line-height: 36px;
  font-size: 25px;
  font-weight: normal;
}

.menu {
  margin-top: 20px;
  padding-left: 0;
  text-align: center;
}

.menu .menu-item {
  display: inline-block;
  margin-left: 20px;

  &:first-child { margin-left: 0; }

  a {
    display: block;
    font-size: 13px;
    text-transform: capitalize;
    line-height: 1.5;
    border-bottom: 1px solid $menu-link-border;
    transition-property: border-color;
    the-transition();
    color : #fff;
    &:hover { border-bottom-color: $menu-link-hover-border; }
  }
}

